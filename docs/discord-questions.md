# Anna Discord — questions to post (you post these; I don't post on your behalf)

Where: https://discord.gg/7ftTdsdMMz, builder-program / support channel. Fallback: reply in
the forum rules thread (https://forum.anna.partners/t/turn-your-ai-agents-apps-into-recurring-monthly-grants-join-the-anna-ai-os-founding-builder-program-up-to-80k-month-pool/205).
Post once, screenshot the answer into docs/decisions.md, add the message link to
docs/growth-log.md → "Program correspondence".

Already answered by the live docs (no need to ask): whether a Node executa can be distributed
without npm (yes — `binary` with `binary_artifacts`, uploaded by `anna-app apps cut`), and
whether a new version needs re-review after approval (no — apps/app-versioning.md).

---

Hi Anna team — I'm building "TokenBrief" (token research briefs, EN + Persian) for the
Founding Builder Program and have a few questions before I invest in growth:

1. Payout channel: the rules list Bank Wire, PayPal, Stripe "or other mutually agreed
   channels". I'm based in Iran, where none of those three work. Would a USDC transfer
   (e.g. on Base or Solana) count as a mutually agreed channel — for tier grants and for
   the 70% revenue share? If not, is there any channel you can support for my
   jurisdiction? I'm happy to complete any verification you require.
2. Is there a KYC/identity step or a minimum age for grant payout, and where is it
   documented?
3. Where do we declare the app's "primary function" for Qualified Run counting — the
   store listing, the registration form, or somewhere else?
4. Do runs started from the app's UI window (tools.invoke + llm.complete) and runs
   started by @mentioning the app in chat both count as Qualified App Runs?
5. Which time zone defines the "calendar month" for MAU?
6. If store review is still pending on 30 Sep, is it fine to submit the DoraHacks BUIDL
   with the pending App link and update it after approval?
7. When an app's executa runs on a Linux Cloud Agent, does each user's agent get its own
   egress IP, or do many agents share one? TokenBrief calls the keyless CoinGecko API, which
   allows about 5 requests a minute per IP, so a shared IP would split that budget between
   all users.

Thanks — will share the app link here once it's live.
